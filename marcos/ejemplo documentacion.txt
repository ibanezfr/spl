import tadTarea, tadListado, tadEmpleados, datetime, tadCola
from datetime import date
from tadTarea import *
from tadListado import *
from tadEmpleados import *
from tadCola import *

def cargarDatos(listadoTareas, listadoEmpleados, cola):

    """
    Carga los datos de 15 empleados con sus tareas, descripcion de la tarea, nombre del empleado, estado de la tarea y fecha de creacion

    Args:
        listadoTareas(): una instancia de la clase tadListado que representa la lista de tareas.
        listadoEmpleados(): Una instancia de la clase tadListado que representa la lista de empleados.
        cola(): Una instancia de la clase tadCola que representa la cola de tareas.

    Returns:
        None
    """

    listaDatosTareasFicticias = [
    [
        "Revisión de requisitos del cliente",
        "Diseñar y desarrollar la interfaz de usuario del software,\n\t\t\t\tasegurando una experiencia de usuario intuitiva.",
        "María López",
        "Pendiente",
        "2024-05-30",
    ]
	# ... Otras tareas